{
  "name": "volo-app-template",
  "version": "1.0.0",
  "description": "Full-stack React + Hono template with Firebase Auth, Neon DB, and Cloudflare deployment",
  "type": "module",
  "scripts": {
    "dev": "node scripts/run-dev.js",
    "firebase:emulator": "firebase emulators:start --only auth --project demo-project --export-on-exit=./data/firebase-emulator --import=./data/firebase-emulator",
    "setup:local": "node scripts/setup-local.js",
    "post-setup": "node scripts/post-setup.js",
    "connect:auth": "echo '🚧 Production Firebase Auth connection not yet implemented. Coming soon!'",
    "connect:database": "echo '🚧 Production database connection not yet implemented. Coming soon!'",
    "connect:deploy": "echo '🚧 Production deployment setup not yet implemented. Coming soon!'",
    "build": "cd ui && pnpm run build",
    "deploy": "cd server && wrangler deploy && echo 'Frontend deployment: Connect your Git repo to Cloudflare Pages'"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "devDependencies": {
    "embedded-postgres": "17.5.0-beta.15",
    "postgres": "^3.4.7",
    "concurrently": "^8.2.2",
    "firebase-tools": "^13.0.0",
    "get-port": "^7.0.0"
  },
  "keywords": [
    "react",
    "hono",
    "firebase",
    "neon",
    "cloudflare",
    "full-stack",
    "template"
  ],
  "template": {
    "placeholders": {
      "WORKER_NAME": "string",
      "FIREBASE_PROJECT_ID": "string",
      "FIREBASE_API_KEY": "string",
      "FIREBASE_MESSAGING_SENDER_ID": "string",
      "FIREBASE_APP_ID": "string",
      "FIREBASE_MEASUREMENT_ID": "string",
      "DATABASE_URL": "string"
    }
  }
}