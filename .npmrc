# Enable scripts for embedded-postgres packages to run their postinstall scripts
# This is needed for proper platform-specific setup (like libzstd on Mac)
enable-scripts=true

# Enable scripts for embedded postgres packages specifically
@embedded-postgres:registry = https://registry.npmjs.org/
@embedded-postgres:always-auth = false

# Alternative: Use pnpm's package-specific script enabling
# This is safer than enabling all scripts globally
enable-scripts = @embedded-postgres/* 